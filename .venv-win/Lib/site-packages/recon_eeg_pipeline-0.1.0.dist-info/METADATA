Metadata-Version: 2.4
Name: recon-eeg-pipeline
Version: 0.1.0
Summary: Realtime BrainCo EEG and gaze policy pipeline for controlled experiments.
Requires-Python: <3.14,>=3.10
Description-Content-Type: text/markdown
Requires-Dist: numpy>=1.23
Requires-Dist: scipy>=1.9
Provides-Extra: dev
Requires-Dist: pytest>=7; extra == "dev"
Requires-Dist: ruff>=0.4; extra == "dev"
Provides-Extra: tobii
Requires-Dist: g3pylib @ git+https://github.com/tobii/glasses3-pylib.git@5868e2d327a077cb6bbe276182188b81fc90f224 ; extra == "tobii"
Provides-Extra: brainco
Requires-Dist: bc-ecap-sdk<0.6,>=0.5; extra == "brainco"
Requires-Dist: zeroconf>=0.47; extra == "brainco"

# Recon 脑电处理流水线

这是原 `eeg_pipeline` 的独立重构区。目标是建立一条可测试、可追踪、可扩展的实时实验闭环：

```text
BrainCo 脑电 ──> 脑电质量与特征 ─────┐
             └─> 原始脑电分块文件    │
                                      ├─> 同步状态 ─> 策略 ─> 实验平台接口
眼动数据源 ────> 眼动特征 ───────────┘
```

旧目录不会被这个项目修改。当前版本已将运行所需的 BrainCo SDK 采集适配器整合进本项目，不再依赖外部 `oi-mi` 目录。

完整运行步骤见 [`docs/USAGE.md`](docs/USAGE.md)；结构性修改和验证记录见 [`docs/MODIFICATION_LOG.md`](docs/MODIFICATION_LOG.md)。

## 当前完成情况

已完成：

- Python 3.9+ 的独立包结构和配置。
- BrainCo 32 通道采集接口。
- 全 32 通道预处理、PSD 与质量评估。
- 可插拔解码器；每个解码器独立选择通道和定义指标。
- 4 秒滚动窗口、滤波、质量门控和相对 Alpha 负荷指标。
- BrainCo 原始 32 通道 EEG 的实时分块、原子落盘与 SHA-256 校验。
- 统一脑电、眼动、多模态状态和策略数据结构。
- 单调时钟与 UTC 双时间戳。
- 数据新鲜度检查和多模态同步器。
- `UnavailableGazeProvider`，缺失 gaze 时不会制造假数据。
- `ReplayGazeProvider`，可在无硬件时验证接口。
- `TobiiG3Provider`，实时接收 Tobii Pro Glasses 3 `gaze2d`，计算
  fixation、saccade、空间熵、有效率和场景 AOI。
- C1/C2/C3 数据源严格隔离。
- 策略连续确认与冷却机制。
- 事件与策略结果的追加式 JSONL 审计日志。
- 面向实验平台的 HTTP API。

有意留作后续实现：

- 基于场景相机标定或外部定位的“网页 DOM AOI”映射。
- 设备时钟漂移估计。
- 将旧实验网页拆分并迁入本项目。
- 在真实硬件环境中验证 BrainCo SDK 和科研阈值。

## 目录

```text
configs/                         运行配置
examples/                        gaze 回放样例
src/recon_pipeline/
├── acquisition/                 硬件采集边界
├── eeg/                         全通道预处理、在线滑窗与可插拔解码器
├── gaze/                        眼动数据源接口、空实现和回放
├── synchronization/             多模态时间同步
├── policy/                      条件隔离与稳定化策略
├── server/                      实验平台 HTTP 适配器
├── storage/                     原始 EEG、实验文档和 JSONL 审计记录
├── application.py               用例协调层
├── clock.py                     统一时间
├── config.py                    配置加载
├── models.py                    核心数据契约
└── runtime.py                   采集后台任务
tests/                           单元与 HTTP 集成测试
```

题库按难度独立存放：

```text
src/recon_pipeline/web/question_bank/
├── easy/question_01.js ... question_16.js
├── medium/question_17.js ... question_32.js
├── hard/question_33.js ... question_48.js
└── index.js                     完整性校验与统一加载入口
```

每道题独占一个文件。文件编号就是稳定的题目 ID，移动或修改题目时无需编辑其他题目的内容。

依赖方向保持单向：

```text
硬件适配器
      ↓
应用服务 ← 多模态同步 ← 数据模型
      ↓
策略引擎
      ↓
HTTP 与存储适配器
```

策略层不读取硬件，HTTP 层不计算脑电，采集层也不知道实验界面的结构。

## 安装和测试

```powershell
cd E:\eeg_pipeline_副本\recon_pipline
C:\Users\ncclab\miniconda3\envs\tobii-g3\python.exe -m pip install -e ".[dev,brainco,tobii]"
C:\Users\ncclab\miniconda3\envs\tobii-g3\python.exe -m pytest
```

## 启动 API

不连接硬件：

```bash
recon-pipeline --config configs/development.json
```

使用眼动回放验证多模态接口：

```bash
recon-pipeline \
  --config configs/development.json \
  --gaze-replay examples/gaze_replay.jsonl \
  --gaze-replay-loop
```

连接 Tobii Pro Glasses 3（自动发现同一网络内的设备）：

```powershell
.\run_tobii.ps1
```

如果网络中有多台设备，或自动发现不可用，请指定眼镜序列号/hostname：

```powershell
$env:G3_HOSTNAME = "tg03b-你的设备序列号"
.\run_tobii.ps1
```

也可以使用 `.\run_tobii.ps1 -TobiiHostname tg03b-你的设备序列号`；若
zeroconf 偶发发现失败，也可传入设备局域网 IP（本次检测为
`.\run_tobii.ps1 -TobiiHostname 169.254.217.102`）。显式地址会跳过自动
发现并直接连接。首次安装 Tobii
依赖时使用：

```powershell
python -m pip install -e ".[dev,tobii]"
```

同时连接 BrainCo 与 Tobii：

```powershell
.\run_tobii.ps1 -BrainCo
```

BrainCo SDK 适配代码已经内置；无需提供 `../oi-armi/oi-mi` 路径。只连接 BrainCo 时可运行
`python -m recon_pipeline.cli --config configs/development.json --brainco`。

Tobii G3 的 `gaze2d` 是眼镜场景相机坐标，而不是浏览器屏幕坐标。当前实时
策略引擎使用凝视持续时间和空间熵；场景九宫格只记录在
`gaze.metadata.scene_region` 中，未标定时 `primary_aoi` 保持为空。只有完成
场景到屏幕平面的标定后，才能将注视点解释为网页元素 AOI。

使用 `--brainco` 时原始 EEG 会自动保存，不需要额外开关。默认目录为
`runs/raw_eeg`，默认每 10 秒写一个 `.npz` 块；题目、实验条件或实验阶段
变化时会提前截断当前块，保证一个块只属于一个上下文。

```text
runs/raw_eeg/S001_20260724_160000_000000/
├── metadata.json
├── manifest.jsonl
└── chunks/
    ├── chunk_000000.npz
    └── chunk_000001.npz
```

具体格式和读取方法见 [`docs/RAW_EEG.md`](docs/RAW_EEG.md)。

默认 API：

```text
http://127.0.0.1:8810
```

页面入口：

```text
http://127.0.0.1:8810/             导航页
http://127.0.0.1:8810/experiment   实验平台
http://127.0.0.1:8810/monitor      脑电、眼动与策略实时监控
```

实验界面的 LLM 请求通过同源代理发送。启动前设置百炼 API Key：

```bash
export DASHSCOPE_API_KEY="你的Key"
```

项目中不保存默认 API Key。

## 最小调用流程

启动实验会话：

```bash
curl -X POST http://127.0.0.1:8810/api/session/start \
  -H 'Content-Type: application/json' \
  -d '{"session_id":"S001","condition":1}'
```

切换到仅眼动条件：

```bash
curl -X POST http://127.0.0.1:8810/api/condition \
  -H 'Content-Type: application/json' \
  -d '{"condition":2}'
```

发送眼动特征：

```bash
curl -X POST http://127.0.0.1:8810/api/gaze \
  -H 'Content-Type: application/json' \
  -d '{
    "status":"available",
    "quality":"pass",
    "primary_aoi":"reading_content",
    "fixation_duration_ms":1100,
    "pupil_dilation":0.72,
    "gaze_entropy":0.68,
    "blink_rate":0.22
  }'
```

查询平台应执行的最新策略：

```bash
curl http://127.0.0.1:8810/api/policy
```

## 实验条件隔离

| 实验条件 | 策略允许使用的数据源 |
|---|---|
| C1 | 不使用 EEG 或 gaze |
| C2 | 只使用眼动；缺失时返回 `hold` |
| C3 | 脑电 + 眼动；默认任一缺失时返回 `hold` |

开发时可将 `policy.allow_degraded_c3` 设置为 `true`，允许 C3 暂时使用 EEG-only。正式实验应保持 `false`。

## 科研边界

- 负荷指标是参与者当前实验会话内基于滚动 Alpha 基线的相对指标。
- 它不是临床、疲劳、表现或跨被试可比的绝对测量。
- 默认 Alpha 解码器使用真实的 `P3/P4/P7/P8/PZ/O1/O2` 通道；可在配置中更换，采集和全通道预处理不受影响。
- 真实实验前必须验证采样单位、通道顺序、阈值和时间同步精度。
