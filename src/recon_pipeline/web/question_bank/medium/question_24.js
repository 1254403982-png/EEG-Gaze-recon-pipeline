window.questionBank = window.questionBank || {};
window.questionBank.medium = window.questionBank.medium || [];
window.questionBank.medium.push(
  { id:24, title:"有限元方法与变分原理", domain:"工程力学", difficulty:"中", diffClass:"is-moderate",
    summary:"有限元方法将复杂的连续体问题离散为有限个单元的组合，通过求解方程组近似得到应力、位移等结果。",
    quickQs:["有限元的基本思想","为什么要把物体切碎","给我出道练习题","用通俗语言解释"],
    content:`<p>工程师要算一座大桥在风载下会变形多少、应力是否超标，但大桥的形状太复杂，无法用纸笔直接写出解析公式。<strong>有限元方法（FEM）</strong>是现代工程分析的利器，它把不可能变成了可能——原理通俗地说就是"化整为零、各个击破"。</p>
<h3>基本思想：化整为零</h3>
<p>有限元的核心思路是：把一个形状复杂的连续体（如桥梁、手机壳、人体骨骼）<strong>切割成许多小块（单元）</strong>，每个小块用简单的形状（三角形、四面体等）近似。小块之间通过节点连接。这样，原本无法求解的复杂连续问题，就变成了大量简单小块的拼装问题。</p>
<div class="formula-block"><span class="formula-label">有限元基本方程</span>$$[K]\{d\} = \{F\}$$<br>$[K]$ = 整体刚度矩阵（由各单元刚度拼装）<br>$\{d\}$ = 节点位移向量（未知，待求）<br>$\{F\}$ = 节点载荷向量（已知外力）</div>
<h3>怎么求解？</h3>
<p>每个单元有自己的"刚度"（抵抗变形的能力），整合成全局刚度矩阵 $[K]$。给定外力 $\{F\}$，求解方程组 $[K]\{d\}=\{F\}$ 就能得到所有节点的位移 $\{d\}$。有了位移，应力和应变也就都能算出来了。</p>
<h3>实际应用</h3>
<p>商业软件（ANSYS、ABAQUS、COMSOL）底层都跑着这套流程。从汽车碰撞仿真、飞机机翼气动弹性分析，到手机跌落测试、心脏支架受力评估，有限元让工程师在造出实物之前就能在电脑里"试错"，大幅降低成本。你手机里几乎每个零件都经过有限元分析。</p>
<h3>变分原理：有限元的数学根基</h3>
<p>为什么"化整为零+拼装求解"能逼近真实答案？背后是<strong>变分原理</strong>：真实的位移场会使系统的<strong>总势能取极小值</strong>（最小势能原理）。但直接找这个全局最小值太难，于是有限元用"分块线性组合"去逼近——把整体位移场表示为各节点位移的加权平均（<strong>形函数</strong>插值），再在约束下求总势能最小。单元切得越细（网格越密），近似越准，但计算量也越大，这里存在精度与成本的权衡。</p>`,
    quiz:[{ question:"有限元方法中，把所有小单元拼装后得到的整体刚度矩阵记为？", options:["A. [K]","B. {d}","C. {F}","D. Nᵢ"], answerIndex:0 },
           { question:"有限元方程 [K]{d}={F} 中，代表节点位移（未知量）的是？", options:["A. 刚度矩阵[K]","B. 位移向量{d}","C. 载荷向量{F}","D. 形函数Nᵢ"], answerIndex:1 }] }
);
